<!--
Thanks for contributing! Delete any section that doesn't apply rather than
leaving it blank — an empty heading reads as "not considered", a deleted one
reads as "not relevant".
-->

## What

<!-- What changes, in one or two sentences. -->

## Why

<!--
The reasoning a reviewer can't recover from the diff: the bug's root cause, the
constraint that ruled out the simpler approach, the issue this closes.
Link issues as "Fixes #123".
-->

## How it was verified

<!--
What you actually ran, and what it printed — not what you expect to pass in CI.

- [ ] `yarn test:typecheck`
- [ ] `yarn test:update`
- [ ] Checked in the browser (say which: editor, excalidraw-app, an example)

For a bug fix, say how you confirmed the bug existed first. A test that fails
before the change and passes after is the strongest form of this.
-->

## Risk

<!--
Delete if it's a docs or test-only change.

- Does this change behaviour for existing `@excalidraw/excalidraw` consumers?
- Does it touch the `.excalidraw` file format or anything persisted?
- Is it feature-flagged, or does it ship to everyone on merge?
-->

## Screenshots / recording

<!-- For anything visual. Before and after, if you can. -->
